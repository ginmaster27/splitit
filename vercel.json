{
  "buildCommand": "npx expo export --platform web && node scripts/inject-web-metadata.js",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
